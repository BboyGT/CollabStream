{
  "name": "companion",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "vite:dev": "vite",
    "vite:build": "vite build",
    "build": "vite build && tauri build --no-bundle",
    "build:bundle": "vite build && tauri build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.2.0"
  }
}
